import React from "react";
import ReactDOM from "react-dom";
import RecruiterSite from "./RecruiterSite";

ReactDOM.render(
  <React.StrictMode>
    <RecruiterSite />
  </React.StrictMode>,
  document.getElementById("root")
);
